#!/bin/sh
set -e

case "$1" in
  purge)
    if getent passwd mlvpn >/dev/null; then
      deluser mlvpn >/dev/null || true
    fi
    if getent group mlvpn >/dev/null; then
      delgroup mlvpn >/dev/null || true
    fi
    ;;
esac

#DEBHELPER#

exit 0
